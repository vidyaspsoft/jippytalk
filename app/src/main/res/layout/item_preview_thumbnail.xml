<?xml version="1.0" encoding="utf-8"?>
<!-- Small thumbnail in the bottom strip of the preview page -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="56dp"
    android:layout_height="56dp"
    android:layout_marginEnd="6dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/cvThumb"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:cardCornerRadius="8dp"
        app:cardElevation="0dp"
        app:strokeWidth="0dp">

        <ImageView
            android:id="@+id/ivThumb"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="@string/todo" />

        <!-- Document icon overlay for non-image files -->
        <ImageView
            android:id="@+id/ivDocThumb"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_document"
            android:visibility="gone"
            android:contentDescription="@string/todo" />

    </androidx.cardview.widget.CardView>

    <!-- Selection border (white ring around selected thumbnail) -->
    <View
        android:id="@+id/vSelectedBorder"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/transparent"
        android:visibility="gone" />

</FrameLayout>
